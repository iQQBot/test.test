{
  "name": "ubuntu-go-node",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "..",
    "args": {
      "USERNAME": "test",
      "USER_UID": "1004",
      "USER_GID": "1004"
    }
  },
  "privileged": true,
  "runArgs": [
    "--pid=host"
  ],
  "postCreateCommand": "go version && node --version && npm --version"
}
